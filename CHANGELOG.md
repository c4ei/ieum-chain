# 변경 기록

## 1.0.5.1 - 2026-08-25

- 4프로세스 BFT 시험의 첫 거래를 `eth_sendRawTransaction` EIP-155 실제 서명 경로로 전환
- 합의 단계 timeout보다 긴 유휴 상태 뒤 첫 거래가 재시작 없이 확정되는 회귀 시험 추가
- 네 노드의 raw 거래 영수증·수신 잔액·확정 nonce·height·state root 일치 검증
- 하루 유휴 운영 시험을 `IEUM_CI_IDLE_WAIT_SECONDS=86400`으로 동일 절차에서 실행 가능
- `--server` 공개 노드의 활동 증명 수집을 합의형 `NodeServiceDailyReward` 지급까지 연결
- 7일 성숙한 100 IEUM 위임 담보, 하루 80% 가동, 독립 검증자 3명 서명을 보상 필수 조건으로 적용
- 메인 검증 서버 제외, 보상 주소 1개당 1노드, IPv4 /24·IPv6 /48당 최대 2노드로 Sybil 공격 비용·중복 제한
- 공개 노드 일일 풀을 재단 잔액 범위와 최대 1,000 IEUM으로 제한하고 검증자별 서명을 합의에서 재검증
- 노드 PeerId 키와 보상용 secp256k1 지갑의 이중 소유권 서명으로 타인 보상 주소 무단 등록 차단

## 1.0.4.1 - 2026-08-25

- round-change에 검증 가능한 valid block과 2/3 서명 prevote 인증서를 전달해 잠금 교착 자동 회복
- 구버전 valid-value 없는 round-change 서명 호환 유지
- 변조된 valid value 및 불충분·중복 prevote 인증서 거부

## 1.0.3.1 - 2026-08-24

- 작은 높이 차이는 연속 확정 블록, 큰 차이·인증서 공백은 2/3 인증 snapshot으로 자동 복구
- 불완전한 동기화 응답이 프로세스를 종료하지 않고 누락 높이부터 재요청
- 기본 3피어 교차검증과 실제 snapshot 영구 재합류 CI 추가
- 키·설정을 보존하는 Docker/systemd 원클릭 복구 쉘 추가
- Docker 이미지에 curl 설치 및 업데이트 후보 이미지 의존성 검사
- 높이가 다른 클러스터는 최저 높이 공통 블록을 네 노드에서 검증한 뒤에만 업데이트
- PR CI 바이너리 공유와 태그 workflow 중복 Clippy/test 제거

## 1.0.2.1 - 2026-08-24

- 지연 노드 동기화 요청·응답을 GossipSub와 독립된 libp2p request-response 경로로 연결
- 연결된 모든 피어에 누락 시작 높이를 직접 요청하고 응답 PeerId를 인증된 연결과 대조
- 기존 GossipSub 동기화 경로는 혼합 버전 호환과 보조 전파를 위해 유지
- 최초 bootstrap Node 1을 기존 원장·영구 PeerId로 재시작하는 3블록 지연 회귀 테스트 추가
- 각 시험 송금의 영수증이 대상 노드에 확정된 뒤 다음 송금을 제출하여 mempool 중복 경합 방지
- `syncHighest` 발견, 네 노드 높이·state root 일치, 생존 피어 요청 수신 로그까지 CI 판정
- 한국어 상태관리·진단·로그 수집·단일 노드 재시작·오류 재현 통합 도구 추가
- 서버·외부 진단 도구에서 직접 동기화 성공·실패 로그를 구분

## 1.0.1.1 - 2026-08-24

- 활성 합의 중 최근 round-change 투표를 2초 간격으로 제한 재전파하여 1노드 장애 뒤 생존 3노드의 라운드 정렬 경합 복구
- 공유 CI runner 지연을 고려하되 실제 높이·잔액으로 판정하는 3노드 확정 대기와 4프로세스 재합류 연속 2회 검증 추가
- Docker/LAN bootstrap 연결 피어를 gossipsub explicit peer로 등록하여 재시작 노드의 sync/consensus/block 토픽 전파 복구
- 마지막 물리 연결 종료 시에만 토픽 피어를 해제하여 다중 연결 경합 방지
- 동기화 요청·수신·응답 한국어 운영 로그 추가
- 실서버용·외부 PC용 한국어 진단 쉘과 `-h` 사용법 추가
- 재합류·진단 도구·릴리스 빌드 정책 회귀 테스트 추가
- 네 번째 문서 개정 번호만 바뀌면 OS별 바이너리 빌드를 생략하고 필요 시 강제 빌드 가능
- 최신 사용자·운영자 매뉴얼과 과거 문서 로컬 백업 정책 정리

## 1.0.0.1 - 2026-08-21

- 실제 4프로세스 BFT 시험에 검증자 중단·3노드 확정·동일 데이터 재기동·자동 재합류 검증 추가
- 재합류 노드의 높이·state root·잔액·과거 영수증 일치 확인
- 표시 버전 `1.0.0.1`과 Cargo 내부 버전 `1.0.0-1`을 릴리스·업데이터에서 호환

## 0.23.12 - 2026-08-21

- `eth_getTransactionCount(address, "pending")`가 해당 발신자의 연속 mempool nonce 포함
- `latest`는 확정 nonce만 유지해 Wallet이 기존 대기 거래를 정확히 감지 가능
- 다른 주소의 대기 거래와 nonce 중간 공백을 섞지 않는 회귀 테스트 추가


## 0.23.11 - 2026-08-21

- 제출되어 mempool에서 확정을 기다리는 거래도 `eth_getTransactionByHash`로 조회
- pending 거래에는 블록 필드를 `null`, `ieumPending=true`로 반환해 확정 거래와 구분
- RPC·노드·mempool·Docker 4노드 상태를 한 번에 확인하는 `ieum-doctor.sh` 추가
- Release의 긴 OS별 빌드 전에 버전·포맷·Clippy·전체 테스트를 한 번 통과하도록 사전검사 분리

## 0.23.10 - 2026-08-21

- BFT 라운드 변경 뒤 runtime 보조 캐시가 비어도 검증 완료된 prevote 기록에서
  `valid_round` 2/3 초과 증명을 다시 구성
- 잠금값과 다른 무증명 블록을 제안하지 않고 거래를 mempool로 안전하게 복원
- 안전 제안 보류 원인을 운영 로그에 표시하여 장기 라운드 교착 진단 개선
- 4검증자 valid-round 증명 캐시 유실·재구성 회귀 테스트 추가

## 0.23.9 - 2026-08-20

- 최대 발행량을 210,000,000 IEUM으로 고정하고 제네시스 합계가 이를 넘지 못하게 검증
- 재단 주소에 최대 발행량의 10%인 21,000,000 IEUM 최초 배분
- PoS·노드 보상은 재단 준비금에서 지급하여 추가 발행 없이 총발행량 보존
- 기존 높이 5 원장을 자동 백업하고 새 제네시스로 시작하는 전환 marker 분리
- 2026-08-10으로 남아 있던 노드 보상 시작값을 새 Genesis 시각으로 이동
- 검증자·위임·보유 이자는 Genesis 다음 KST 날짜부터만 생성·검증

## 0.23.8 - 2026-08-20

- Ethereum legacy raw 거래 조회 시 원본 `gasPrice`와 `gasLimit`을 정확히 복원
- IEUM 자체 서명 거래는 `gas=1`, `gasPrice=fee`로 표시해 조회 수수료 합계를 보존
- 거래 영수증에 `gasUsed`, `effectiveGasPrice`, `logs`, `logsBloom` 표준 필드 추가
- 확정 뒤 RPC 재시작 시 잔액·nonce·거래 영수증이 동일하게 복원되는 회귀 테스트 추가
- 재단 20%·블록 생성자 80% 수수료 배분과 운영 기본기 전용 테스트 스크립트 추가

## 0.23.7 - 2026-08-19

- 제안의 `valid_round`에 해당하는 2/3 초과 서명 prevote 인증서를 함께 전파하고 검증
- 다른 값에 잠긴 노드는 인증서 라운드가 자기 잠금 라운드 이상일 때만 안전하게 전환
- 제안 서명 도메인을 `IEUM-PROPOSAL-V3`로 올리고 인증서 서명 목록까지 결합
- 합의 topic을 `ieum-chain/consensus/2`, Identify protocol을 `/ieum-chain/1.2.0`으로 분리
- 4검증자·5P2P 노드의 의미와 LAN/DNS 광고 주소 운영 기준 문서화
- 자동 업데이트 manifest를 서명된 최신 GitHub Release 자산에서 조회
- GitHub Linux release 작업의 APT 재시도·타임아웃을 제한해 무기한 대기 방지

## 0.23.6 - 2026-08-19

- 합의 단계별 timeout 시 블록 투표와 분리된 검증자 서명 round-change를 P2P로 전파
- 더 높은 라운드에서 1/3 초과 투표권을 확인하면 catch-up하고, 2/3 초과 서명
  round-change를 확인하면 다음 라운드로 진행
- 피어 재연결 시 최근 round-change 투표를 제한적으로 재전파하여 장기 분리 복구
- 제안자 여부와 무관하게 대기 거래를 2초 간격으로 재전파하여 일부 노드 mempool에만
  거래가 남는 상황 완화
- 무등록 검증자 라운드 점프 거부, 라운드 catch-up 및 bounded timeout 회귀 테스트 추가

## 0.22.4 - 2026-08-12

- 제네시스 설정 시각을 실제 0번 블록 timestamp에 반영
- 제네시스→1번 구간을 블록 생산 통계에서 제외
- 설정된 `--block-time-ms`를 지연 및 누락 슬롯 계산 기준으로 사용

## 0.22.2 - 2026-08-12

- Manager 토폴로지 자동 발견용 읽기 전용 `ieum_peerInfo` RPC 추가
- 연결 피어 주소·방향·연결 수·연결 시간을 상태에 안전하게 추적
- 비밀키와 서명 재료는 피어 응답에서 제외

## 0.22.1 - 2026-08-12

- 공급량·주소 잔액 인덱스·validator 서명률·블록 생성 품질 RPC 추가
- Prometheus exporter, Grafana 대시보드와 Telegram/이메일 장애 알림 구성 추가
- 민감한 원문을 기록하지 않는 관리자 작업 감사 로그 추가

## 0.21.13 - 2026-08-11

- P2P 블록 수신 로그를 `PeerId/height/hash/tx count/system event count` 요약으로 변경
- 같은 블록을 여러 피어에서 받아도 프로세스당 최초 1회만 운영 로그에 기록
- 블록 수신 로그에서 raw transaction과 서명을 제거하고 중복 기억을 4,096개로 제한
- 블록 익스플로러용 JSON-RPC 응답 필드와 raw 서명 비노출 회귀 테스트 추가
- 표준 JSON-RPC 요청 디스패치 회귀 테스트 추가
- 운영·계정·송금·익스플로러 연동을 통합한 사용자 매뉴얼 1차 완료본 추가

## 0.21.12 - 2026-08-11

- 모든 상대 경로를 셸의 현재 폴더가 아닌 실행 중인 `ieum-chain` 바이너리 폴더 기준으로 고정
- 한 서버의 `/opt/ieum-node1`, `node2`, `node3`가 설정·원장·키·계정을 완전히 분리
- 자동 업데이트 설정과 교체 대상을 현재 바이너리 인스턴스로 제한
- 기본 계정 암호를 각 인스턴스의 `secure/ieum-account.password`에 0600으로 자동 생성
- `account new/import/send`가 별도 옵션 없이 자기 인스턴스의 암호와 keystore 사용

## 0.21.7 - 2026-08-05

- `server --git_action_test`에서만 루프백 전용 CI 네트워크와 고정 개발 키를 활성화
- 운영 제네시스는 유지하고 CI 실행 중에만 테스트 계정 4개에 각각 1 IEUM을 배정
- 4프로세스 CI에서 네 노드의 faucet 1 IEUM을 먼저 검증하고 0.1 IEUM을 전송
- JSON-RPC 오류, 거래 해시, 네 노드의 수신 잔액·높이·상태 루트를 성공 조건으로 검사
- P2P·합의·보상 서명 키를 `data/keys/` 아래의 직관적인 이름으로 정리
- 기존 키 파일은 내용을 재생성하지 않고 시작 시 새 경로로 자동 이전

## 0.21.6 - 2026-08-05

- 실제 4프로세스 BFT CI 노드를 노드별 빈 임시 작업 디렉터리에서 실행
- CI가 저장소의 운영용 `config/network.json`과 `config/update.json`을 읽어
  운영 DNS·공개 광고 주소·자동 업데이트에 접근하던 경로 차단
- 테스트 실행 파일을 절대 경로로 고정하고 종료 시 임시 노드 프로세스와 상태 정리
- Node emission 기간 조건을 Rust 1.97 Clippy 권장 범위 검사로 변경

## 0.21.4 - 2026-08-04

- 100 IEUM 송금 테스트 잔액이 포함된 운영 제네시스 식별자를
  `657ce0cfeb8ad38d88a23711ec2664e5e1033aa8ffd5bb648a02ca0f348a9e1a`로 고정
- 체인과 월렛 릴리스가 서로 다른 제네시스 해시를 사용하지 않도록 회귀 테스트 추가
- 조기 도착한 BFT 투표를 보류 후 재처리하여 실제 4프로세스 합의 CI의 순서 경쟁 수정
- 단일 서버 4노드의 node1~3에도 자동 업데이트 신뢰 설정을 함께 설치

## 0.21.3 - 2026-08-04

- 옵션 없는 기본 실행을 안전한 자동 역할 선택으로 변경
- 고급 사용자용 `--mode auto|client|public|validator` 추가
- 검증자 키와 승인 목록이 일치할 때만 자동 검증자 참여
- AutoNAT 결과를 공개 지원 가능/일반 클라이언트 유지 상태로 표시
- 송금 테스트 주소 제네시스 100 IEUM 및 단일 서버 4노드 PeerId 수정 포함

## 0.21.0 - 2026-08-03

- 운영망 신원·프로토콜·동기화·최종 확정 블록·복구 상태 RPC 추가
- 원본 거래 해시를 보존하는 거래 복구 기록 조회 추가
- 체크포인트 롤백은 검증자 수와 투표권 모두 3/4 이상일 때만 승인

## 0.20.9 - 2026-08-03

- 해킹·오발행처럼 영향 범위가 명확한 사고는 원본 거래를 삭제하지 않고 승인된
  보상 기록을 남기는 거래 단위 복구를 기본 원칙으로 정했습니다.
- 원장 전체 손상 또는 합의 버그에서만 체크포인트 롤백을 최후 수단으로 사용하도록
  운영 기준을 분리했습니다.
- `ieum-chain recovery policy` 명령으로 운영 서버에서 두 복구 방식의 선택 기준과
  금지 사항을 즉시 확인할 수 있습니다.
- 복구 계획 해시에 서명한 등록 검증자 수 3/4 이상 또는 전체 투표권 3/4 이상 중
  하나를 충족하면 승인되는 공통 판정 로직을 추가했습니다.
- 중복 서명은 한 번만 계산하고 미등록 검증자의 승인은 오류로 거부합니다.
- 확정 거래 직접 삭제, 단일 관리자 임의 잔액 변경 및 검증자 합의 없는 전체 롤백은
  지원하지 않음을 사용자 매뉴얼에 명시했습니다.

## 0.20.8 - 2026-07-31

- 검증자 자동 등록 시 임시 설정 파일 이름을 프로세스별로 고유하게 생성하고
  디스크 동기화 후 원자적으로 교체하여 `validators.json.tmp` 저장 경쟁을
  제거했습니다.
- 4프로세스 BFT 테스트가 노드별 `validators.json`을 사용하도록 분리했습니다.
- `--no-default-bootstrap` 옵션을 추가하여 CI·폐쇄형 개발망이 운영 bootstrap에
  접속하지 않도록 격리했습니다.
- 설치, 업데이트, 백업·복구, `node doctor`, P2P·AutoNAT 로그 판별과 CI 실행을
  v0.20.8 기준 사용자 매뉴얼로 통합·보강했습니다.

## 0.20.7 - 2026-07-31

- 서버 시작 시 자기 광고 주소의 오래된 PeerId를 현재 `server.node.key` 기준으로
  자동 보정하고, 일반 `node clean`에서는 노드·검증자 키를 보존합니다.
- `node doctor`, 안전한 원장 백업 정리, 검증자 키 소유권을 요구하는 잔액 이전
  명령을 추가하고 4프로세스 테스트 상태 폴더를 노드별로 분리했습니다.
- 운영 7003 bootstrap PeerId를 현재 서버 키의 PeerId로 교체했습니다.
- Identify로 학습한 `/dns4` 주소도 직접 연결 전 IPv4로 변환합니다.
- 목적지 PeerId 중복 및 다단계 `/p2p-circuit` 주소를 정규화·차단하여 릴레이
  경로가 재귀적으로 늘어나는 문제를 막았습니다.
- P2P 상태와 RPC의 피어 수를 QUIC 연결 개수가 아닌 고유 PeerId 수로 집계하고,
  로그에는 고유 피어 수와 같은 피어의 연결 수를 분리해 표시합니다.
- 토픽 가입 전 `NoPeersSubscribedToTopic`은 장애 오류가 아닌 전파 대기 상태로
  집계합니다.

## 0.20.5 - 2026-07-31

- 기본 부트스트랩을 `node.ieum.aah.name`의 UDP 7001·7002·7003 운영 노드로 고정
- `network show/set/reset` 명령으로 부트스트랩과 서버별 공개 광고 주소 관리
- NAT 포트포워딩의 외부 포트가 로컬 리스닝 포트와 달라도 명시적으로 광고
- 자동 업데이트를 시작 직후 1회 확인하고 이후 6시간마다 확인

## 0.20.4 - 2026-07-31

- 운영체제의 기본 외부 라우팅에 사용되는 로컬 IPv4를 확인해 NIC에 공인 IPv4가 직접
  할당됐는지 시작 로그에 표시합니다.
- NIC 공인 IP 보유 여부와 실제 외부 역접속 가능 여부를 분리했습니다. 최종 외부
  접근성은 AutoNAT 결과로 `외부 직접 접근 가능`, `NAT/방화벽 내부`, `판정 대기`로
  안내합니다.
- mDNS로 같은 LAN에서 실제 발견된 PeerId에만 사설 IPv4 주소 학습을 허용합니다.
  외부 피어의 Identify 정보에 포함된 `127.x`, Docker/VMware/에어포트·핫스팟 등
  다른 사설망 주소는 Kademlia 재접속 후보에서 제외하고 기존 항목도 제거합니다.
- 공인 bootstrap DNS 주소는 계속 AutoNAT 서버·릴레이·직접 연결의 우선 주소로
  유지하고, 연결 피어가 0명이 되면 60초마다 학습 주소가 아닌 원본 bootstrap DNS로
  자동 재접속합니다.
- `Unexpected peer ID` 로그가 무조건 `server.node.key` 오류로 오해되지 않도록,
  잘못 학습된 로컬/사설 주소와 실제 bootstrap 키 불일치를 구분해 안내합니다.
- 공인/사설/CGNAT 주소 분류와 동일 LAN 피어 주소 허용 정책 테스트를 추가했습니다.

## 0.20.3 - 2026-07-31

- Circuit Relay v2 클라이언트·서버와 DCUtR 홀 펀칭을 통합해 NAT·CGNAT·동일
  공인 IP 아래의 다중 VM/채굴장 노드가 매번 포트를 추가 개방하지 않아도 연결할
  수 있게 했습니다.
- 일반 노드는 운영 부트스트랩 노드에 릴레이 예약을 요청합니다. 직접 연결이
  가능하면 DCUtR로 승격하고, 불가능하면 릴레이 연결을 유지합니다.
- 공개 부트스트랩 노드는 별도 실행 모드 없이 릴레이 서버와 AutoNAT 판정 서버로
  동작합니다. 기존 UDP 7001 포트 개방은 이 공개 노드에만 필요합니다.
- 같은 LAN의 노드는 기존 mDNS와 사설 IP 직접 연결을 우선 사용합니다.
- AutoNAT 접근성 판정, 릴레이 예약, 홀 펀칭 결과를 운영 로그에 구분해 표시하고,
  릴레이·NAT 연결의 유휴 제한을 120초로 늘렸습니다.
- 변경 파일만 적용한 소스에서도 패키징 스크립트가 신규 libp2p 전이 의존성을
  `Cargo.lock`에 추가한 뒤 기존 `--locked` 검증을 수행하도록 했습니다.

## 0.20.2 - 2026-07-31

- systemd의 작업 디렉터리나 사용자의 실행 위치가 달라도 실행 파일 설치 경로의
  `config/update.json`을 우선 찾아 자동 업데이트를 활성화합니다.
- 노드가 시작되면 5분을 기다리지 않고 서명된 manifest를 즉시 확인하며, 이후에는
  설정된 주기로 계속 확인합니다.
- GitHub raw/CDN의 이전 manifest 캐시를 피하도록 검사 URL에 고유 query와 no-cache
  헤더를 추가했습니다. manifest 서명과 업데이트 바이너리 SHA-256 검증은 그대로
  유지합니다.
- 자동 업데이트 설정 경로, 시작 즉시 확인 여부, 실제 검사 시점을 운영 로그에
  표시합니다.

## 0.20.1 - 2026-07-31

- 검증자 4명 자동 등록 직후 이미 `Prevote`로 넘어간 합의 라운드에서 로컬 제안을
  다시 처리해 노드가 `"현재 단계에서는 블록을 제안할 수 없습니다."` 오류로
  종료되던 문제를 수정했습니다.
- 현재 단계가 `Propose`이고 현재 노드가 라운드 제안자인 경우에만 블록 제안을
  생성합니다. 제안 처리 도중 단계가 바뀌어도 거래를 복원하고 다음 합의 tick에서
  재시도하며 서버 프로세스를 종료하지 않습니다.
- 검증자 등록은 피어 연결 직후 즉시 전송하되 반복 heartbeat를 2초에서 60초로
  낮춰 동일 등록 메시지와 로그가 과도하게 누적되는 문제를 줄였습니다.
- `Prevote` 이후 중복 로컬 제안을 만들지 않는 회귀 테스트를 추가했습니다.

## 0.19.9 - 2026-07-30

- 설치, 실행, systemd, 백업, 복구, 자동 업데이트, 장애 확인을 한 문서로 정리한
  사용자 매뉴얼을 추가했습니다.
- 반복 P2P·검증자 로그가 carriage return으로 다른 비동기 로그와 한 줄에 섞이던
  문제를 수정했습니다. 최초와 10·100·1000회 누적 시점만 독립된 새 줄로 남깁니다.
- 노드 배포 압축에 현재 버전 사용자 매뉴얼을 포함합니다.
- 패키징 시 Ed25519 개인키로 업데이트 manifest를 자동 생성·서명하고,
  업데이트 실행파일·SHA-256·활성 `config/update.json`을 함께 생성합니다.
- 설치 패키지가 공개키가 고정된 자동 업데이트 설정을 설치하도록 변경했습니다.
- 무인 자동 업데이트와 BFT/거버넌스 결정을 분리하고 검증자 순차 배포 원칙,
  v0.19.8 트래픽 정책의 실제 활성화 전 조건, DEX/브리지 권장 경계를 문서화했습니다.

## 0.19.8 - 2026-07-30

- 근거리·지역/ASN 다양성·저부하 슬롯을 결합한 결정론적 피어 선택 코어 추가
- 국가·ASN·중앙 부트스트랩별 연결 상한으로 특정 거점의 트래픽 독점 방지
- 검증된 중계 영수증의 중복 제거, 교차 확인, 확인자·노드별 기여 상한 추가
- 확정 블록 seed와 제곱근 가중치를 쓰는 일~주 단위 이벤트 추첨 코어 추가
- 운영 기본 정책과 실제 P2P 영수증·BFT 지급 활성화 전 보안 시험 계획 문서화

## 0.19.7 - 2026-07-30

- 높이 0에서 서명 확인된 서버 4명이 모여도 제네시스 설정 때문에 후보 대기에
  머물던 부트스트랩 검증자 전환 조건 수정
- P2P 버전 알림과 로컬 고정 공개키 기반의 서명 manifest 자동 업데이트 추가
- 업데이트 실행 파일 SHA-256 검증, 이전 실행 파일 백업 및 서비스 재시작 연동
- 최초 활성 검증자 4명에게 각 10 IEUM을 BFT 확정 후 한 번만 지급
- 서로 다른 노드 100개 이상 확인 시 등록 노드별 1 IEUM을 한 번만 지급
- 영구 `data/reward.key`와 `reward address`, `reward send` 명령 추가

## 0.19.2 - 2026-07-29

- `ieum-chain server`가 별도 옵션 없이 `node.ieum.aah.name` 운영 부트스트랩에 연결합니다.
- 각 서버가 검증자 공개키와 PeerId의 소유권 서명을 P2P로 반복 공지합니다.
- 제네시스 구간에서 유효한 검증자 4명이 모이면 결정론적 공통 집합을 저장하고 BFT를
  자동 시작합니다.
- 거래나 예약 이벤트가 없는 유휴 상태에서는 불필요한 BFT 라운드를 올리지 않습니다.
- 실제 합의 타임아웃이 발생하면 터미널의 같은 줄에서 라운드 숫자만 갱신합니다.

## 0.19.1 - 2026-07-29

- 전 세계에 첫 노드만 있는 제네시스 상태에서도 `ieum-chain server`가 즉시 실행됩니다.
- 첫 실행에서 생성한 검증자 공개키 1개로 `config/validators.json`을 자동 생성합니다.
- 검증자 4명 미만은 개발·초기 구성용 부트스트랩 합의임을 시작 로그에 표시합니다.
- 기존 운영 설정의 4명 이상 조건은 다음 epoch 검증자 세트 활성화 단계에 유지합니다.

## 0.19.0 - 2026-07-29

- 개발망·CI에서 고정 테스트 검증자 4명의 공통 설정을 자동 생성합니다.
- 운영 검증자 선발 정책 코어를 추가했습니다.
- 유통량 1% 이상 보유·잠긴 위임 지분, 국가가 인증된 계좌의 국가별 IEUM 보유량 상위
  50위, 관리자 승인 중 하나로 후보 자격을 계산합니다.
- 계좌 소유권 서명, 외부 QUIC 접속 확인, 95% 가동률을 공통 필수 조건으로 둡니다.
- 차단 후보를 항상 제외하고 동일 지분은 공개키 순으로 정렬해 모든 노드가 같은 결과를
  계산합니다.

## 0.18.0 - 2026-07-29

- 지갑 WebRTC 계층을 위한 peer 간 암호화 통신 신호 전달 코어를 추가했습니다.
- 통화 초대·수락·거절, offer/answer, ICE, 종료, 암호화 채팅 종류를 지원합니다.
- 통신 신호는 120초 이내 만료, 64KiB 이하 암호문만 허용하고 중복 id를 거부합니다.
- 실제 발신 PeerId와 메시지 발신자가 다르면 거부하며 대상 peer만 수신합니다.
- `ieum_sendCommunication`, `ieum_pollCommunication` 로컬 RPC를 추가했습니다.
- 영상·음성·채팅 평문은 블록, 원장, DB에 저장하지 않습니다.
- IEUM 송금은 기존 `eth_sendTransaction`과 `eth_sendRawTransaction`을 지갑 UI에서 사용합니다.

## 0.17.1 - 2026-07-29

- `server` 최초 실행 시 검증자 키, 노드 키, 원장 폴더와 빈 운영 설정을 자동 생성합니다.
- 초기화 표시 이후 키 또는 원장이 사라지면 새 신원을 만들지 않고 안전하게 중단합니다.
- 기본 검증자 설정 경로를 운영용 `config/validators.json`으로 변경합니다.
- 초보자용 Windows/Ubuntu 실행 안내와 peer 통신 보안 설계를 분리해 정리합니다.

## 0.17.0 - 2026-07-29

- 서명된 업데이트 manifest와 SHA-256을 검증하고 일반 사용자에게 설치 여부를 질문합니다.
- 검증자 서버는 업데이트를 자동 교체하지 않고 알림만 출력합니다.
- 블록 높이가 아닌 합의 블록 시각을 사용하는 예약 이벤트와 체크포인트 블록을 추가합니다.
- 재단 배분, 주간 생성자 보상, 사고 보상 역거래, 프로토콜 체크포인트를 지원합니다.
- 이벤트 ID 실행 기록을 상태·스냅샷에 저장해 정확히 한 번만 실행합니다.
- 기존 v0.16.x 블록의 시스템 이벤트 목록이 비어 있으면 기존 블록 해시를 유지합니다.

## 0.16.1 - 2026-07-28

- 확정 블록의 각 거래 수수료를 검증자 80%, 재단 20%, 소각 0%로 분배합니다.
- 재단 수수료 주소를 `0x356456ff1216b57a6f8891b195b42d296789b67d`로 합의 코드에 고정했습니다.
- 재단 몫은 내림 계산하고 나머지를 검증자에게 지급해 수수료 유실을 막습니다.
- 수수료 총액 보존, 재단 적립, 작은 수수료 반올림을 회귀 테스트로 검증합니다.

## 0.15.2 - 2026-07-28

- JSON 상태 파일을 SQLite WAL 기반 실제 저장 엔진으로 교체했습니다.
- 기존 JSON 상태를 최초 실행 시 자동 이관하고 원본을 보존합니다.
- RPC 병렬 테스트가 각자 고유한 SQLite 경로를 사용하도록 수정했습니다.

## 0.15.1 - 2026-07-28

- 계정별 다중키와 키별 권한을 추가했습니다.
- 작업별 가중치 임계값과 고액 송금 별도 임계값을 추가했습니다.
- 정책 nonce로 오래된 권한 서명의 재사용을 차단합니다.
- 대기 높이와 복구키 임계값을 사용하는 계정 복구 요청을 추가했습니다.
- 외부 signer/HSM 경계와 함께 사용할 수 있는 공개키 정책 모델을 추가했습니다.

## 0.14.1 - 2026-07-28

- pruned/archive 운영 모드와 최근 상태·체크포인트 보존 규칙을 추가했습니다.
- schema·chain id·state root를 포함한 backup/restore manifest를 추가했습니다.
- `/metrics`, `/healthz` HTTP Router를 추가했습니다.
- 기존 WAL embedded DB를 유지하고 backend 전환용 manifest 경계를 추가했습니다.
- RocksDB/SQLite 네이티브 backend는 잠금파일 생성·실컴파일이 가능한 환경에서 후속 적용합니다.

## 0.13.1 - 2026-07-28

- bank/staking/governance/identity/reward 전용 state prefix를 추가했습니다.
- 합의와 애플리케이션 상태 전이를 분리하는 모듈 trait/router를 추가했습니다.
- 정렬된 상태 key 기반 결정론적 state root를 추가했습니다.
- 모듈별 연속 상태 migration 검증을 추가했습니다.

## 0.12.1 - 2026-07-28

- 서명된 nil prevote/precommit을 추가했습니다.
- 2/3 초과 nil precommit으로 검증하는 round-change certificate를 추가했습니다.
- era 길이와 활성화 지연을 가진 검증자 세트 관리자를 추가했습니다.
- 검증자 세트 변경은 지연 후 다음 era 경계에서만 적용합니다.

## 0.11.2 - 2026-07-28

- Rust 1.97 rustfmt/clippy 경고 수정을 기준 소스에 포함했습니다.
- 신규 코어 기능마다 단위 테스트를 추가하고 패키지·잠금파일 버전을 일치시켰습니다.

## 0.11.1 - 2026-07-28

- snapshot chunk 병렬 할당, 피어별 timeout·재시도·failover 스케줄러
- embedded DB WAL 우선 복구와 fsync checkpoint
- 외부 signer timeout, 호출 간격 제한, 다중 signer HA failover
- Prometheus text 형식의 노드 핵심 metrics 수집기
- nil/round-change certificate의 후속 네트워크 결합 기준 문서화

## 0.10.1 - 2026-07-28

- snapshot chunk별 SHA-256 검증과 다운로드 재개 지점 영속화
- 서로 다른 2~3개 피어의 height/block hash/state root quorum 검증
- 원자적 embedded key-value DB와 canonical 상태 우선 복원
- 로컬 키와 외부 signer/HSM adapter를 분리하는 검증자 서명 경계
- 외부 signer 응답 서명을 공개키로 재검증

## 0.9.1 - 2026-07-28

- Tendermint 계열 locked value / valid value 및 valid round 제안 규칙
- propose, prevote, precommit 단계별 timeout
- 서명된 외부 이중투표 증거 검증·영속 저장·P2P 전파
- 실제 QUIC P2P 네 개 프로세스의 높이·state root 통합 테스트
- 동일한 개발 제네시스를 위한 결정론적 테스트 faucet

## 0.8.2 - 2026-07-28

- 동기화 응답 누적 바이트를 `usize`로 명시해 Rust 숫자 타입 추론 오류를 제거했습니다.
- GitHub Actions에서 fmt, clippy, 전체 테스트, release build를 자동 검사합니다.
- `four_node_bft`, 인증서 동기화, WAL, peer guard, v0.8.1 회귀 테스트가
  모든 push와 pull request에서 실행됩니다.
- ROADMAP의 오래된 현재 제한을 실제 v0.8.2 상태에 맞게 정리했습니다.

## 0.8.1 - 2026-07-28

- BFT 확정 실행, 인증서 동기화, 상충 확정 인증서 거부를 강화했습니다.
- state root·잔액·nonce·블록·거래 인덱스를 원자적으로 저장합니다.
- 비밀번호 기반 암호화 사용자 keystore를 추가했습니다.
- mempool 용량·교체·수수료 우선 정책을 추가했습니다.
- 실제 피어 수·동기화 진행률과 운영 상태 RPC를 추가했습니다.
- 높이 기반 프로토콜 업그레이드 일정을 추가했습니다.

## 0.6.4 - 2026-07-27

### 수정

- `consensus_runtime` 모듈을 라이브러리에 공개해 통합 테스트 import 오류를 수정했습니다.
- 개인키를 가진 `Wallet`에 `Clone`을 추가하지 않고, 4노드 테스트가 테스트 seed에서
  노드별 지갑을 재생성하도록 수정했습니다.
- 운영 JSON-RPC 기본 포트를 `127.0.0.1:8989`로 변경했습니다.
- 패키지 버전과 문서의 현재 버전을 `0.6.4`로 일치시켰습니다.

### 운영

- 공개 RPC는 8989 포트를 직접 노출하지 않고 Caddy reverse proxy를 통해 제공합니다.
- 한 서버에서 노드를 여러 개 실행할 때 P2P 포트와 RPC 포트를 모두 다르게 지정합니다.

## 0.6.1 - 2026-07-24

### 수정

- `src/lib.rs`의 위조 서명 테스트에서 한글을 ASCII 전용 byte string
  (`b"..."`)에 넣어 컴파일되지 않던 문제를 수정했습니다.
- `"위조 서명".as_bytes()`를 사용해 UTF-8 바이트를 서명 함수에 전달합니다.

### 문서

- 모바일·웹 지갑이 필요할 때만 접속하고 작업 후 연결을 끊는 라이트
  클라이언트 구조를 `docs/CLIENT.md`에 추가했습니다.
- README, 아키텍처와 로드맵에 간헐 접속 클라이언트 계획을 반영했습니다.

## 0.6.0

- 서명된 BFT 투표와 검증을 추가했습니다.
- 합의 WAL 저장·복구와 로컬 이중서명 방지를 추가했습니다.
- 주요 소스에 한국어 학습 주석을 보강했습니다.
# 0.6.3

- Kademlia 이벤트를 명시적으로 소비해 정상 기동 시 발생하던 `dead_code` 경고를 제거했습니다.
- 배포 압축본과 실행 바이너리의 버전을 0.6.3으로 일치시켰습니다.
# v0.6.3 RPC 실행 보완 - 2026-07-27

- 기존에 파일로만 존재하던 `rpc.rs`를 라이브러리와 노드 실행 경로에 연결
- `--rpc-port`, `--rpc-host`, `--rpc-data-dir` 옵션 추가
- 기본 `127.0.0.1:8545`에서 지갑용 JSON-RPC 동시 실행
- RPC 관련 모듈과 `axum`, BIP-32/39, secp256k1, Keccak 의존성 연결
- RPC 서버 조기 종료 시 노드가 오류 원인을 반환하도록 처리
# v0.15.2

- JSON 전체 image 저장소를 실제 SQLite key-value 저장 엔진으로 교체
- SQLite WAL, `synchronous=FULL`, 5초 busy timeout 및 원자적 batch transaction 적용
- v0.15.1 `db/ieum-state.db`를 최초 실행 시 자동 이관하고
  `db/ieum-state.legacy-v1.json`으로 원본 보존
- backup 전에 WAL checkpoint를 실행하고 `ieum-state.sqlite3`를 복사하도록 변경
- DB schema/generation metadata 및 prefix scan 테스트 추가
## 0.21.5 - 2026-08-05

- Windows Normal용 SQLite 정적 링크 수정
- Normal relay 최소 1시간 가동 자격 추가
- 높이 100과 2026-08-10 00:00 KST를 모두 충족하는 보상 활성화 조건 추가
- 2026~2040 노드 보상 총량 210,000,000 IEUM 및 매년 반감 예산 추가
- 메인 4노드 1.5배 가중치와 기존 전송 수수료 20% 재단 배분 정책 문서화
# 0.21.11

- CLI와 `personal_*` RPC가 동일한 `data/keystore` 다중 계정 저장소를 사용합니다.
- `account import`, `account balance`, `account transaction`, `account receipt`를 추가했습니다.
- keystore 파일명을 `UTC--timestamp--address` 형식으로 누적하며 기존 `address.json`도 읽습니다.
- 계정 생성·raw key import·IEUM 전송·잔액 및 트랜잭션/영수증 조회 흐름을 문서화했습니다.
# 0.23.5

- IEUM 메인넷 이름을 `ieum-mainnet`으로 확정하고 공개 개발키 4개 주소를 Genesis에서 제거했습니다.
- GitHub Actions는 Chain ID `21005`의 별도 `config/genesis_test.json`을 사용해 기존 송금 테스트 주소 4개의 1 IEUM 잔액을 유지합니다.
- Genesis 시각을 2026-08-19 00:00 KST, 공급량을 80,100 IEUM, 공식 hash를 `c7a4f99b...ff8a5a`로 고정했습니다.
- 자동 업데이트된 바이너리가 번들 Genesis 설정을 원자적으로 동기화하고 기존 파일을 백업합니다.
- 저장 원장과 월별 체크포인트의 Genesis hash가 현재 메인넷과 다르면 시작을 거부합니다.
- Genesis와 향후 발행을 합한 전역 최대 공급량을 210,000,000 IEUM으로 고정했습니다.
- 일반 보유 보상의 확정 지급 내역을 조회하는 `ieum_holderRewardHistory` RPC를 추가했습니다.
- 최초 배분 공개 절차, 참여 보상, 메인넷 전환과 사고 대응 훈련 문서를 추가했습니다.
